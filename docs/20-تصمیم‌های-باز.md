# تصمیم‌های بازِ FlowStudio

قاعده: **پیش از نوشتنِ کدِ هر بخش، تصمیم‌های آن بخش باید بسته باشد.** هر تصمیم
یک شناسهٔ یکتا (`D-xxx`) دارد؛ در کامیت‌ها با همان شناسه ارجاع می‌دهیم.

وضعیت: `باز` · `بسته` · `منتفی`

> 🔄 **۱۸ مرداد (سشنِ سوم):** شماره‌گذاریِ قدیمی (۱ تا ۱۲) تصادم داشت — مثلاً
> «۲» هم «باریکهٔ اول» بود هم «اولین مشتری»، «۶» هم «تأمینِ مدل‌ها» (بسته) بود
> هم «بودجه» (باز). `docs/flowstudio-execution-charter.pdf` این را پیدا کرد.
> از این‌جا به بعد فقط شناسهٔ `D-xxx` معتبر است؛ شماره‌های قدیمی دیگر ارجاع
> داده نمی‌شوند.

---

## بسته‌شده

| شناسه | تصمیم | نتیجه | چه‌وقت |
|---|---|---|---|
| D-C01 | استقلالِ پروژه | مخزنِ جدا، کانتینرِ جدا، دیتابیسِ جدا. از ماژول‌های وبکستینگ **کد** برداشته می‌شود، نه **سرویس**. | ۱۸ مرداد |
| D-C02 | استک | Next.js 14 برایِ وب. API: NestJS + Prisma + PostgreSQL. | ۱۸ مرداد |
| D-C03 | فازِ ۰ | فقط صفحهٔ فرود + فرمِ تماس. هیچ دیتابیسی. سرنخ‌ها در JSONL. | ۱۸ مرداد |
| D-003 | محلِ اجرا | فعلاً ایران؛ هتزنر بعد از تست‌های اولیه. مهاجرتِ بعدی = افزودنِ یک ردیف در رجیستری، نه بازنویسی. | ۱۸ مرداد |
| D-C04 | تأمینِ مدل‌ها | توکن از تجمیع‌کننده‌ها (9router، آوالای، گپ‌جی‌پی‌تی…)، پرداخت به‌ازای مصرف. درگاه = یک ردیفِ داده. | ۱۸ مرداد |
| D-009 | دامنه | `flowstudio.ir` ثبت و روی Cloudflare است ولی **دست‌نخورده می‌ماند** فعلاً. محصول روی `flowstudio.webcasting.ir`. ⚠️ زمان‌بندیِ مهاجرت هنوز باز — `D-012`. | ۱۸ مرداد |
| D-C11 | گسترهٔ محصول | پوزیشنینگ از روزِ اول کلِ طیف (چند ثانیه تا فیلمِ کامل)؛ اجرا از کوتاه شروع می‌شود؛ مدلِ داده از روزِ اول طول‌آگاه. جایگزینِ «لِینِ اول»ِ قدیمی هم هست. | ۱۸ مرداد، سشنِ ۲ و ۳ |
| D-C05 | ادغامِ Falak | یک موتور، دو الگوی محصول. جزئیات `docs/50` نیازِ ۱۰. | ۱۸ مرداد، سشنِ ۳ |
| D-005 | واحدِ پول | **سکه** (`coin`) — کپیِ الگوی وبکستینگ. `Wallet.balance Int`. | ۱۸ مرداد — دستورِ مالک |
| D-007 | صف | **جدولِ `Job` + قفلِ دیتابیسی** (`UPDATE…WHERE status='PENDING' RETURNING`) — نه BullMQ. | ۱۸ مرداد — دستورِ مالک |
| D-008 | احرازِ هویت | **موبایل + OTP** — مستقل از وبکستینگ، مثلِ فیلمتو. | ۱۸ مرداد — دستورِ مالک |
| D-010 | C2PA | **واترمارکِ متنی** — کافی برایِ الان؛ امضایِ واقعی بعداً اگر مشتریِ اروپایی آمد. | ۱۸ مرداد — دستورِ مالک |
| D-O14 | واحدِ حساب (تعارضِ سندِ ۸۱ با D-005) | **دفتر سکه‌ای و `Int` می‌ماند.** `Decimal` فقط `listPricePerUnit`ِ رجیستری است — قیمت‌نامه و ورودیِ CPAS، نه کسر از کیفِ پول. دلیل: `Int` و `Decimal` در یک دفترِ مالی یعنی خطای گِردکردن در `hold/settle`. | ۱۹ مرداد — دستورِ مالک |
| D-O15 | مبنایِ تصمیمِ روتر: `modality` یا `stepTypes` | **هر دو.** `modality` فیلترِ درشت می‌ماند، `stepTypes String[]` ریز اضافه می‌شود؛ اگر پر بود مبنا اوست، وگرنه به `modality` می‌افتد. روترِ v1 و پنلِ ادمین نمی‌شکنند. | ۱۹ مرداد — دستورِ مالک |
| D-101 | نوع‌های تولید نسخه اول | **تبلیغی، فیلم کوتاه، مستند** — همان سه نوع فعال در رجیستری. نوع چهارم یک ردیف داده است. | ۲۰ مرداد |
| D-201 | کلیپ پایه شاخه بازیگر و فایل ورودی دوبله | **فقط آپلود خود کاربر، با تعهد صریح مالکیت.** همان قابلیت فنی، بدون گره حقوقی. | ۲۰ مرداد |
| D-203 | انتشار خودکار یا با تایید راهبر | **تایید پیش از انتشار.** برداشتن دروازه بعدا یک تغییر سیاست است؛ افزودنش پس از یک اثر مشکل‌دار، یک بحران. | ۲۰ مرداد |
| D-207 | ناظر انسانی کیست | **مالک، با مهلت ۲۴ ساعت و رفتار پیش‌فرض «در صف بماند».** رد خودکار کاربر را بی‌دلیل می‌سوزاند؛ تایید خودکار گیت را باطل می‌کند. | ۲۰ مرداد |
| D-208 | منبع موسیقی | **کتابخانه مجوزدار برای برنزی و نقره‌ای، مولد برای طلایی.** موسیقی مولد هم پرونده مجوز می‌خواهد. | ۲۰ مرداد |
| D-210 | زبان‌های نسخه اول | **فارسی زبان پایه؛ عربی و انگلیسی نسخه مشتق.** زبان از روز اول فیلد داده است، نه رشته در پرامپت. | ۲۰ مرداد |
| D-211 | نسخه زبانی: مشتق یا اثر جدا | **مشتق**، با الگوی «یک اثر، چند نمایش». نمایش به نسخه اشاره می‌کند، نه مستقیم به پروژه. | ۲۰ مرداد |
| D-212 | زیرنویس سوخته یا جانبی | **هر دو.** سوخته برای شبکه اجتماعی، جانبی برای پخش‌کننده و ترجمه. هزینه هر دو تقریبا صفر. | ۲۰ مرداد |
| D-213 | نوع موزیک ویدیو | **غیرفعال بماند** تا تشخیص ضرب ساخته شود — پلانش وارونه است. | ۲۰ مرداد |
| D-214 | صدای دوبله | **صدای بومی تازه.** شبیه‌سازی صدا رضایت صریح می‌خواهد، همان دسته رضایت چهره. | ۲۰ مرداد |
| D-215 | جداسازی گفتار از موسیقی | **روی سرور خودمان** — هزینه‌اش پردازنده ماست نه پول درگاه. عددش یک بار اندازه گرفته شود. | ۲۰ مرداد |
| D-216 | هم‌گامی لب در دوبله | **پیش‌فرض خاموش**، فقط سطح طلایی با انتخاب صریح. ۰٫۰۷ دلار بر ثانیه گران‌تر از خود ویدیوست. | ۲۰ مرداد |
| D-217 | موتور رندر متن روی تصویر | **کانتینر کارگر جدا با مرورگر بی‌سر و ابزار ویدیو**، تا ایمیج اصلی سبک بماند. | ۲۰ مرداد |
| D-218 | تیک کارگر صف | **حلقه داخل خود پروسه کارگر** — قاعده «هیچ کرونی داخل API» نمی‌شکند. | ۲۰ مرداد |
| D-O13 | آستانه داوری خودکار در نردبان تشدید | **دستی بماند.** داور خودکار پیش از جمع شدن داده، سلیقه را به قاعده تبدیل می‌کند. | ۲۰ مرداد |
| D-209 | مجوز دیپلوی | **انجام شد.** شواهد: `evidence/deploy-2026-08-10.txt`. | ۱۹ مرداد |

---

## باز — پاسخِ مالک لازم است

شش تصمیم مانده. هیچ‌کدام با «تایید پیشنهاد» بسته نمی‌شود، چون هر کدام یک
**اقدام** یا یک **متن** یا یک **عدد** یا یک **نام** لازم دارد، نه یک انتخاب.

| شناسه | چه چیزی لازم است | جنسش | کدام اسپرینت را نگه داشته |
|---|---|---|---|
| — | **کلید واقعی تجمیع‌کننده** ثبت شود در `/admin/models` | اقدام | اسپرینت ۱ |
| D-205 | **متن قوانین**، فهرست ممنوعه‌ها، شرط سنی | متن | اسپرینت ۶ |
| D-206 · D-03B | **خرید حداقل اعتبار روی دو نامزد** میزبان مدل باز | اقدام و پول | اسپرینت ۷ |
| D-006 | **عدد بودجه نود روز** | عدد | اسپرینت ۷ |
| D-202 | **رابط فنی فلک**: API دارد؟ حساب آپلود داریم؟ چه کسی آن سمت را می‌سازد؟ | واقعیت | اسپرینت ۱۲ |
| D-204 | **قیمت سه سطح** — ورودی‌اش عدد اسپرینت ۷ است | عدد مشتق | اسپرینت ۱۴ |
| D-002 | **نام اولین مشتری واقعی** | نام | روی اولویت اثر دارد، نه روی کد |

### شرح تصمیم‌هایی که اسپرینت‌های بعدی را بلوکه می‌کنند

| شناسه | تصمیم | اثر |
|---|---|---|
| **D-03B** | کدام تجمیع‌کننده مدلِ ویدیو دارد و شکلِ APIاش چیست | اسپرینتِ ۵ تا ۸ در حدِ طراحی می‌مانند. حل با یک `curl` واقعی — `docs/60`. 🔄 **۲۱ مرداد: نیمهٔ «شکلِ API» حل شد** بدونِ کلید — ۲۲ آداپترِ مرجعِ `submit→poll→fetch` در `~/Projects/OpenMontage`، تفکیکِ کامل در [`docs/82`](82-اتصالِ-OpenMontage-به-فازِ-۶.md). ✅ **۲۲ مرداد: جدولِ حقیقتِ هر ۱۴ مسیرِ ناهمگام استخراج شد** — [`docs/83`](83-قراردادِ-آداپترِ-ویدیوی-ناهمگام.md). نیمهٔ «کدام تجمیع‌کننده» همچنان باز و منتظرِ مالک — تجمیع‌کننده‌های ایرانیِ D-C04 در آن مخزن نیستند |
| **D-O17** | جداییِ `rejected` (ردِ سیاستِ محتوا) از `failed` (خرابیِ فنی) در حلقهٔ failover | 🔴 بدونِ این تفکیک، یک ردِ `NSFW` سه بار تکرار و سه بار پول می‌شود. پیش‌فرضِ پیشنهادی: **تفکیک انجام شود**، `rejected` مستقیم به گیتِ `D-203` برود نه به failover — [`docs/83` بخشِ ۵](83-قراردادِ-آداپترِ-ویدیوی-ناهمگام.md) |
| **D-O16** | نحوهٔ استفاده از OpenMontage (پروانهٔ **AGPLv3**) | 🔴 فلواستودیو سرویسِ پولیِ شبکه‌ای است و بندِ ۱۳ی AGPL می‌تواند کلِ آن را مشمولِ افشای کد کند. پیش‌فرض: **گزینهٔ الف — مرجعِ خواندنی**، بازنویسی در TypeScript. **تا بسته‌نشدن هیچ خطی وارد فلواستودیو نمی‌شود.** — [`docs/82` بخشِ ۴](82-اتصالِ-OpenMontage-به-فازِ-۶.md) |
| **D-004** | کدام مدلِ ویدیو در روزِ اول (Veo 3 یا Runway Gen-4؛ Kling غیرفعال) | وابسته به D-03B |
| **D-002** | اولین مشتریِ واقعیِ کدام سایتِ وبکستینگ | بدونِ نام، «تولیدِ ویدیو» همیشه دمو می‌ماند |
| **D-006** | بودجهٔ ۹۰ روزِ اول (سقفِ خرجِ مدل‌ها + زیرساخت) | بدونِ عدد، اسپرینتِ ۳ به بعد (پرداختی) شروع نمی‌شود |
| **D-009q** | منبعِ قواعدِ کلاسِ E | ✅ انجام شد — `knowledge/rules/E-class-v1.md`، ده قاعده از راهنمایِ رسمیِ ABCDِ گوگل |
| **D-012** | زمان‌بندیِ مهاجرت به `flowstudio.ir` | ⏳ عملاً معلق: دامنه در ایرنیک «در انتظارِ تأیید» است، نیم‌سرور از قبل روی Cloudflare تنظیم شده |
| **D-O12** | مهاجرت از میزبانیِ مشترک (سندِ ۸۱ بخشِ ۱۲) | از «باز» به **«برنامه‌ریزی‌شده»**: مجاز وقتی قراردادِ جدایش سبز باشد (گرپِ تمیز — ۱۹ مرداد سبز بود) و یک بار در staging تست شده باشد |
| **D-O13** | آستانهٔ داوریِ خودکار در نردبانِ تشدید | تا بسته‌نشدن، نردبان **دستی** می‌ماند — که خودِ سندِ ۸۱ برایِ MVP قابلِ قبول می‌داند. بلوکرِ فوری نیست |

---

## کاری که بدونِ هیچ تصمیمی انجام شد/می‌شود

طبقِ `docs/flowstudio-execution-charter.pdf` بخشِ ۱۰:
1. ⏳ پروبِ دستیِ تجمیع‌کننده (حلِ D-03B و D-004) — نیاز به کلیدِ واقعی دارد، هنوز نشده.
2. ⏳ رجیستریِ مدل به‌عنوانِ دادهٔ خام (YAML/CSV) — هنوز نشده؛ دادهٔ دقیقِ مدل‌ها را باید مالک تأیید کند تا حدس زده نشود.
3. ✅ تایپ‌هایِ IR در `packages/ir` — انجام شد، با آزمونِ پذیرشِ واقعی.
4. ✅ ده قاعدهٔ کلاسِ E — انجام شد، `knowledge/rules/E-class-v1.md`.
