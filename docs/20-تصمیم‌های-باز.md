# تصمیم‌های بازِ FlowStudio

قاعده: **پیش از نوشتنِ کدِ هر بخش، تصمیم‌های آن بخش باید بسته باشد.** هر تصمیم
یک شناسهٔ یکتا (`D-xxx`) دارد؛ در کامیت‌ها با همان شناسه ارجاع می‌دهیم.

وضعیت: `باز` · `بسته` · `منتفی`

> 🔄 **۱۸ مرداد (سشنِ سوم):** شماره‌گذاریِ قدیمی (۱ تا ۱۲) تصادم داشت — مثلاً
> «۲» هم «باریکهٔ اول» بود هم «اولین مشتری»، «۶» هم «تأمینِ مدل‌ها» (بسته) بود
> هم «بودجه» (باز). `docs/flowstudio-execution-charter.pdf` این را پیدا کرد.
> از این‌جا به بعد فقط شناسهٔ `D-xxx` معتبر است؛ شماره‌های قدیمی دیگر ارجاع
> داده نمی‌شوند.

---

## بسته‌شده

| شناسه | تصمیم | نتیجه | چه‌وقت |
|---|---|---|---|
| D-C01 | استقلالِ پروژه | مخزنِ جدا، کانتینرِ جدا، دیتابیسِ جدا. از ماژول‌های وبکستینگ **کد** برداشته می‌شود، نه **سرویس**. | ۱۸ مرداد |
| D-C02 | استک | Next.js 14 برایِ وب. API: NestJS + Prisma + PostgreSQL. | ۱۸ مرداد |
| D-C03 | فازِ ۰ | فقط صفحهٔ فرود + فرمِ تماس. هیچ دیتابیسی. سرنخ‌ها در JSONL. | ۱۸ مرداد |
| D-003 | محلِ اجرا | فعلاً ایران؛ هتزنر بعد از تست‌های اولیه. مهاجرتِ بعدی = افزودنِ یک ردیف در رجیستری، نه بازنویسی. | ۱۸ مرداد |
| D-C04 | تأمینِ مدل‌ها | توکن از تجمیع‌کننده‌ها (9router، آوالای، گپ‌جی‌پی‌تی…)، پرداخت به‌ازای مصرف. درگاه = یک ردیفِ داده. | ۱۸ مرداد |
| D-009 | دامنه | `flowstudio.ir` ثبت و روی Cloudflare است ولی **دست‌نخورده می‌ماند** فعلاً. محصول روی `flowstudio.webcasting.ir`. ⚠️ زمان‌بندیِ مهاجرت هنوز باز — `D-012`. | ۱۸ مرداد |
| D-C11 | گسترهٔ محصول | پوزیشنینگ از روزِ اول کلِ طیف (چند ثانیه تا فیلمِ کامل)؛ اجرا از کوتاه شروع می‌شود؛ مدلِ داده از روزِ اول طول‌آگاه. جایگزینِ «لِینِ اول»ِ قدیمی هم هست. | ۱۸ مرداد، سشنِ ۲ و ۳ |
| D-C05 | ادغامِ Falak | یک موتور، دو الگوی محصول. جزئیات `docs/50` نیازِ ۱۰. | ۱۸ مرداد، سشنِ ۳ |
| D-005 | واحدِ پول | **سکه** (`coin`) — کپیِ الگوی وبکستینگ. `Wallet.balance Int`. | ۱۸ مرداد — دستورِ مالک |
| D-007 | صف | **جدولِ `Job` + قفلِ دیتابیسی** (`UPDATE…WHERE status='PENDING' RETURNING`) — نه BullMQ. | ۱۸ مرداد — دستورِ مالک |
| D-008 | احرازِ هویت | **موبایل + OTP** — مستقل از وبکستینگ، مثلِ فیلمتو. | ۱۸ مرداد — دستورِ مالک |
| D-010 | C2PA | **واترمارکِ متنی** — کافی برایِ الان؛ امضایِ واقعی بعداً اگر مشتریِ اروپایی آمد. | ۱۸ مرداد — دستورِ مالک |
| D-O14 | واحدِ حساب (تعارضِ سندِ ۸۱ با D-005) | **دفتر سکه‌ای و `Int` می‌ماند.** `Decimal` فقط `listPricePerUnit`ِ رجیستری است — قیمت‌نامه و ورودیِ CPAS، نه کسر از کیفِ پول. دلیل: `Int` و `Decimal` در یک دفترِ مالی یعنی خطای گِردکردن در `hold/settle`. | ۱۹ مرداد — دستورِ مالک |
| D-O15 | مبنایِ تصمیمِ روتر: `modality` یا `stepTypes` | **هر دو.** `modality` فیلترِ درشت می‌ماند، `stepTypes String[]` ریز اضافه می‌شود؛ اگر پر بود مبنا اوست، وگرنه به `modality` می‌افتد. روترِ v1 و پنلِ ادمین نمی‌شکنند. | ۱۹ مرداد — دستورِ مالک |

---

## باز — پاسخِ مالک لازم است

✅ **مسدودکنندهٔ اسپرینتِ ۰ رفع شد** (D-005, D-007, D-008, D-010 بالا بسته شدند).
اسپرینتِ ۰ از این‌جا به بعد شروع می‌شود.

### تصمیم‌هایی که اسپرینت‌های بعدی را بلوکه می‌کنند

| شناسه | تصمیم | اثر |
|---|---|---|
| **D-03B** | کدام تجمیع‌کننده مدلِ ویدیو دارد و شکلِ APIاش چیست | اسپرینتِ ۵ تا ۸ در حدِ طراحی می‌مانند. حل با یک `curl` واقعی — `docs/60` |
| **D-004** | کدام مدلِ ویدیو در روزِ اول (Veo 3 یا Runway Gen-4؛ Kling غیرفعال) | وابسته به D-03B |
| **D-002** | اولین مشتریِ واقعیِ کدام سایتِ وبکستینگ | بدونِ نام، «تولیدِ ویدیو» همیشه دمو می‌ماند |
| **D-006** | بودجهٔ ۹۰ روزِ اول (سقفِ خرجِ مدل‌ها + زیرساخت) | بدونِ عدد، اسپرینتِ ۳ به بعد (پرداختی) شروع نمی‌شود |
| **D-009q** | منبعِ قواعدِ کلاسِ E | ✅ انجام شد — `knowledge/rules/E-class-v1.md`، ده قاعده از راهنمایِ رسمیِ ABCDِ گوگل |
| **D-012** | زمان‌بندیِ مهاجرت به `flowstudio.ir` | ⏳ عملاً معلق: دامنه در ایرنیک «در انتظارِ تأیید» است، نیم‌سرور از قبل روی Cloudflare تنظیم شده |
| **D-O12** | مهاجرت از میزبانیِ مشترک (سندِ ۸۱ بخشِ ۱۲) | از «باز» به **«برنامه‌ریزی‌شده»**: مجاز وقتی قراردادِ جدایش سبز باشد (گرپِ تمیز — ۱۹ مرداد سبز بود) و یک بار در staging تست شده باشد |
| **D-O13** | آستانهٔ داوریِ خودکار در نردبانِ تشدید | تا بسته‌نشدن، نردبان **دستی** می‌ماند — که خودِ سندِ ۸۱ برایِ MVP قابلِ قبول می‌داند. بلوکرِ فوری نیست |

---

## کاری که بدونِ هیچ تصمیمی انجام شد/می‌شود

طبقِ `docs/flowstudio-execution-charter.pdf` بخشِ ۱۰:
1. ⏳ پروبِ دستیِ تجمیع‌کننده (حلِ D-03B و D-004) — نیاز به کلیدِ واقعی دارد، هنوز نشده.
2. ⏳ رجیستریِ مدل به‌عنوانِ دادهٔ خام (YAML/CSV) — هنوز نشده؛ دادهٔ دقیقِ مدل‌ها را باید مالک تأیید کند تا حدس زده نشود.
3. ✅ تایپ‌هایِ IR در `packages/ir` — انجام شد، با آزمونِ پذیرشِ واقعی.
4. ✅ ده قاعدهٔ کلاسِ E — انجام شد، `knowledge/rules/E-class-v1.md`.
