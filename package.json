{
  "name": "flowstudio",
  "version": "0.1.0",
  "private": true,
  "description": "FlowStudio — مونوریپوی api/engine/media + پکیج‌های مشترک. apps/web عمداً بیرونِ این workspace می‌ماند (دیپلویِ مستقلِ زنده، دست‌نخورده).",
  "workspaces": [
    "apps/api",
    "packages/*"
  ],
  "scripts": {
    "db:push": "npm run db:push --workspace=@flowstudio/api",
    "start:dev": "npm run start:dev --workspace=@flowstudio/api"
  }
}
